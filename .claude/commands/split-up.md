Split the given file up into smaller modules and remember to put them in the proper location based on my project structure. Always co-locate components in the route if possible instead of putting them in far off directories. Things such as shadcn dialogs or modals should live in their own components. Custom hooks should be made to consoliate logic related to components. Cohesion should be kept high, meaning put related functions in the same file that is using them if they are not shared functions or components. The content of different tabs should live in their own components. Forms and all their logic should live in their own components:
