- Change the job's done mp3 to actually use a hook to get the original user prompt so that we can read out the job which we finished.
- Add a hook which plays a description of the current feature we are planning to add in.
